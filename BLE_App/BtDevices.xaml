<?xml version="1.0" encoding="utf-8" ?>
<!--Main user interface for app, likely to be the first page users open. This page will be where the users scan and connect to our bluetooth device-->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BLE_App.BtDevices"
             Title="Select BLE Device">

    <Grid>
        <StackLayout Orientation="Vertical">
            <!-- ListView is the list that will be filled with the BLE devices detected -->
            <ListView x:Name="foundBleDevicesListView"
                      Footer=""
                      ItemTapped="foundBleDevicesListView_ItemTapped"/>
        </StackLayout>
        <!-- ActivityIndicator to show a loading indicator when scanning -->
        <ActivityIndicator x:Name="IsBusyIndicator"
                           Scale="0.3"/>
    </Grid>

</ContentPage>


